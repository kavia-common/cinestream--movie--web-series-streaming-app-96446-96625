# Frontend environment variables for CineStream

# PUBLIC_INTERFACE
# REACT_APP_API_BASE_URL is used by the frontend to contact the FastAPI backend.
# Set this to the base URL of your backend (without trailing slash).
# Example local dev (FastAPI on port 3001):
#   REACT_APP_API_BASE_URL=http://localhost:3001
REACT_APP_API_BASE_URL=http://localhost:3001
