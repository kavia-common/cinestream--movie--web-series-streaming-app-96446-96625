# Frontend environment variables

# Base URL of the Backend API (no trailing slash).
# For local development with FastAPI on port 3001:
# REACT_APP_API_BASE_URL=http://localhost:3001
REACT_APP_API_BASE_URL=
