# CineStream Frontend Environment Variables (do not commit .env with secrets)

# Base URL of the Backend API (e.g., https://api.cinestream.example.com or http://localhost:8000)
REACT_APP_API_BASE_URL=http://localhost:8000

# Public key for Stripe Elements (if using built-in checkout on frontend)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_12345

# The site URL of this frontend (used for redirects in auth/payment flows)
REACT_APP_SITE_URL=http://localhost:3000
